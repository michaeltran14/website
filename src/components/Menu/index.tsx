import { IconButton } from "@mui/material";
import { FiMenu } from "react-icons/fi";

const Component = () => {
  return (
    <IconButton aria-label="menu" color="secondary" size="large">
      <FiMenu />
    </IconButton>
  );
};

export default Component;
